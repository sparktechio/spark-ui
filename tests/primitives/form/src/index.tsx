import React from 'react';
import ReactDOM from 'react-dom/client';
import {Example} from "@sparkui/react-form";

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);
root.render(
  <Example />
);
